name: Recognition Gate 4 Fast

on:
  push:
    branches:
      - m9/recognition-trustworthiness-v1
    paths:
      - "src/htcn/harmonic/**"
      - "scripts/m9_recognition_real_market_disagreement_gate.py"
      - "scripts/m9_recognition_real_market_identity_diagnostic.py"
      - "research/a-share-research-universe-v1.json"
      - ".github/workflows/recognition-gate4-fast.yml"
  workflow_dispatch:

concurrency:
  group: htcn-recognition-gate4-fast-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate4:
    name: recognition-gate4-fast
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore pinned A-share research snapshots
        id: recognition-snapshot-cache
        uses: actions/cache/restore@v4
        with:
          path: artifacts/ci-research/data
          key: m2-research-qfq-${{ runner.os }}-${{ hashFiles('research/a-share-research-universe-v1.json') }}
          restore-keys: |
            m2-research-qfq-${{ runner.os }}-

      - name: Bootstrap frozen snapshots on cache miss
        if: steps.recognition-snapshot-cache.outputs.cache-hit != 'true'
        uses: actions/download-artifact@v4
        with:
          name: m2-autonomous-research-80421ff54cb0338906a3d34e5242740ec2d3469d
          repository: shazhuya/HT-CN
          run-id: 35114589602
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path: artifacts/ci-research

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install pinned Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.lock
          python -m pip install --no-deps -e .

      - name: Run Gate 4 real-market disagreement audit
        run: python -u scripts/m9_recognition_real_market_disagreement_gate.py

      - name: Upload Gate 4 report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: htcn-recognition-gate4-${{ github.sha }}
          path: artifacts/reports/m9-recognition-real-market-disagreement.json
          if-no-files-found: error
          retention-days: 30

      - name: Run Gate 4 identity rejection diagnostic
        run: python -u scripts/m9_recognition_real_market_identity_diagnostic.py

      - name: Upload Gate 4 identity diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: htcn-recognition-gate4-identity-${{ github.sha }}
          path: artifacts/reports/m9-recognition-real-market-identity-diagnostic.json
          if-no-files-found: error
          retention-days: 30

